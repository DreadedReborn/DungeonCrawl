Start

Define "timer" as integer, equals a random number between 1 and 2000.
Define "timeMax" as integer, equals 2000

Every time "act" is called
	if "timer" = 0 then
		place "doorclosed.class" at location
		remove "self"
	else "timer"--
end if